---
title: "TAG-DLM: Diffusion Language Models for Text-Attributed Graph Learning"
title_zh: TAG-DLM：面向文本属性图学习的扩散语言模型
authors: "Lingjie Chen, Yuanchen Bei, Haobo Xu, Yanjun Zhao, Yuzhong Chen, Hanghang Tong"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31166v1"
tags: ["query:graph-llm"]
score: 10.0
evidence: 在掩码扩散语言模型中统一文本推理和图消息传递，用于文本属性图
tldr: 文本属性图（TAG）的节点包含自然语言描述，现有方法常分离文本编码与图结构处理，限制了语义与拓扑的深度融合。TAG-DLM提出基于掩码扩散语言模型的新范式，将采样邻域线性化为令牌序列，并通过拓扑注意力掩码实现图消息传递与文本推理的统一。实验在三个基准的两项任务上全面超越图神经网络、图变换器及大语言模型基线，最高提升3.9个百分点。该方法仅需调整提示即可完成节点分类、链接预测及跨数据集迁移，无需针对下游任务微调。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31166v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 778, \"height\": 786, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31166v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1626, \"height\": 477, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31166v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1666, \"height\": 725, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31166v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 803, \"height\": 329, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31166v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 812, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31166v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 660, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31166v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 798, \"height\": 310, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31166v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 903, \"height\": 214, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31166v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 442, \"height\": 251, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31166v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 802, \"height\": 1247, \"label\": \"Table\"}]"
motivation: 现有方法将文本编码与图结构学习分离，难以深度融合语义与拓扑，限制了文本属性图上的联合推理能力。
method: TAG-DLM基于掩码扩散语言模型，将局部邻域线性化为令牌序列，并利用拓扑注意力掩码实现图消息传递，统一文本推理与结构学习。
result: 在三个TAG基准上，TAG-DLM在节点分类和链接预测任务中均超越图神经网络、图变换器及大语言模型基线，最高提升3.9个百分点。
conclusion: TAG-DLM展示了扩散语言模型在图学习中的潜力，仅通过改变提示即可泛化到多个任务，无需微调，实现高效跨任务迁移。
---

## 摘要
文本属性图（TAGs）中每个节点携带一个自然语言描述，要求模型能够对文本和图拓扑进行联合推理。现有方法通常分别处理这两种模态：图神经网络基于浅层文本特征运行，而大语言模型与图的混合方法主要将语言模型作为文本编码器，并将结构学习委派给单独的图模块。我们提出一种方法，在掩码扩散语言模型内统一文本推理和图消息传递，该扩散语言模型是一种具备双向注意力与生成式解码的语言模型。对于每个图实例，该方法将采样的局部邻域线性化为标记序列，并通过拓扑注意力掩码注入图结构，从而在图之上实现消息传递。由于扩散语言模型既能解释文本也能生成文本，该方法仅需更改提示即可适应不同任务，支持节点分类、链接预测和跨数据集迁移，无需针对特定目标进行微调。实验表明，该方法在两个任务的所有三个TAG基准上均优于图神经网络、图变换器和基于大语言模型的基线，相较于最强基线提升高达3.9个百分点。

## Abstract
Text-attributed graphs (TAGs), where each node carries a natural language description, require models to jointly reason over text and graph topology. Existing approaches often handle the two modalities separately: graph neural networks operate on shallow text features, while hybrids of LLMs and graphs use the language model mainly as a text encoder and delegate structure learning to a separate graph module. We propose method that unifies textual reasoning and graph message passing within a masked diffusion language model, a language model with bidirectional attention and generative decoding. For each graph instance, method linearises a sampled local neighbourhood into a token sequence and injects graph structure through a topology attention mask, which realises message passing over the graph. Because the diffusion language model can both interpret and generate text, the method adapts to different tasks simply by changing the prompt, supporting node classification, link prediction, and cross-dataset transfer with no target-specific fine-tuning.   Experiments show that method outperforms graph neural networks, graph transformers, and LLM-based baselines on all three TAG benchmarks across two tasks, improving over the strongest baseline by up to 3.9 points.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：文本属性图（TAG）要求模型同时理解节点的自然语言描述与图拓扑结构。现有方法普遍将文本编码和图结构推理分离为两个独立模块（如 GNN 直接处理浅层文本特征，LLM+GNN 将 LLM 作为文本编码器再接入独立的图模块），导致语义与拓扑无法深度融合，且不同任务需要不同的输出头和特定训练。
*   **整体含义**：该论文提出一个统一框架 TAG-DLM，将图消息传递嵌入到掩码扩散语言模型（MDLM）的双向注意力中。语言模型本身成为图算子，文本证据与拓扑结构在同一个去噪网络中交互，仅需改变提示（prompt）即可适应节点分类、链接预测和跨数据集迁移，无需为特定任务重新设计输出头或调整架构。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
*   **核心思想**：将 TAG 学习转化为掩码填充问题。对每个目标节点或边，采样其局部 k-hop 邻域，并将目标节点与邻居节点的文本线性拼接成一个标记序列。图结构通过一个与序列绑定的“拓扑注意力掩码”注入到 MDLM 的双向自注意力中，使得注意力计算同时实现文本理解和图结构消息传递。
*   **关键技术细节**：
    *   **序列线性化**：目标节点连同其采样的邻居按模板顺序拼接为序列 `Sv = T(v) ∥ T(u1) ∥ ... ∥ T(unv)`，答案位置用 `[M]` 掩码标记。
    *   **拓扑注意力掩码**：对序列中的每个节点（标记段）施加星形注意力模式。目标段可关注所有邻居段，邻居段仅关注自身和目标段，邻居之间不直接通信，形成以目标为中心的聚合。
    *   **掩码扩散语言模型**：骨干网络为 LLaDA-8B（带有双向注意力），通过 LoRA 适配器进行轻量微调。训练时，最大化掩码位置的交叉熵（对应去噪目标）。推理时，通过迭代去噪逐步恢复掩码标记，最终从答案位置的标记 logits 中选择得分最高的选项。
*   **理论等价性**：论文从理论上证明，在固定扩散时间下，使用拓扑掩码的一个双向自注意力层等价于一个基于注意力的图消息传递层（式（7）-（8））。当注意力分数采用加性形式时，可恢复图注意力网络（GAT）的标准更新。整个反向扩散过程可视为在掩码诱导的图上的多次消息传递操作。

### 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法
*   **数据集与任务**：
    *   **节点分类**：Cora（7类）、PubMed（3类）、ogbn-arxiv（40类，大规模）。
    *   **链接预测**：相同三个数据集。
    *   **跨数据集迁移**：在源数据集上训练后，不经微调直接评估在另外两个目标数据集上的性能。
*   **对比方法**：
    *   **图神经网络**：GCN、GraphSAGE、GAT、GATv2、GIN、MixHop。
    *   **图变换器**：GraphTransformer、NodeFormer、DIFFormer、SGFormer。
    *   **语言模型基线**：纯文本 RoBERTa；当前 SOTA 的 LLM-GNN 混合方法 LLaGA；以及未经微调的冻结 LLaDA-8B（TAG-DLM 的骨干网络）。
*   **评估指标**：分类准确率。

### 4. 资源与算力
*   **硬件**：单节点 8 张 NVIDIA A100 80GB GPU。
*   **训练时间**：Cora 约 4 小时，PubMed 约 12 小时，ogbn-arxiv 约 50 小时。加上节点分类和链接预测分别训练，主要领域内训练总时长约 1056 GPU 小时。
*   **训练配置**：LoRA 微调（秩 64），优化器 AdamW，学习率 5e-5，有效批次大小 128，80 epochs（论文中写20，可能是勘误，根据上下文应为20），采用 DeepSpeed ZeRO-2 和 bfloat16 精度。

### 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平
*   **实验组数**：约 32 组主要实验结果（不计不同种子）。覆盖 3 个数据集 × 2 个任务的主流比较（每项约 12 个基线方法），以及 9 种跨数据集迁移设置。此外，进行了拓扑掩码消融（3 数据集 × 2 任务）、掩码类型消融（2 数据集 × 2 任务）、邻居数量消融（3 数据集 × 2 任务 × 多个 K 值）等 20+ 组消融实验。
*   **充分性和公平性**：对比基线全面，涵盖经典 GNN、图变换器和最新 LLM 混合方法。所有基线均经过验证集调参。对于 LLM 基线，统一使用与 TAG-DLM 相同的邻域采样和提示格式，保证了比较的公平性。设计上注意了标签泄漏（邻居标签掩码、边移除）等关键细节。论文还补充了种子设置和完整超参数表格，增加了可复现性。整体实验设计充分且客观。

### 6. 论文的主要结论与发现
*   在两个任务、三个不同规模与语义的图上，TAG-DLM 均取得最优准确率，尤其在链接预测任务上优势显著，最高提升 3.9 个百分点。
*   不需要修改架构，仅通过改变提示就能统一支持节点分类、链接预测和跨数据集迁移，且跨数据集迁移能力显著强于冻结的骨干模型。
*   拓扑注意力掩码是核心有效组件，移除后性能全面下降；若使用暴露邻居间真实边（ego-graph）的掩码，性能反而不如星形掩码，表明目标为中心的简单聚合更稳定。
*   采样邻域大小需适中：默认 K=10 效果最佳，过多无关邻居会引入噪声。

### 7. 优点：方法或实验设计上有哪些亮点
*   **架构统一性**：首次利用 MDLM 的双向注意力一次性完成文本理解和图消息传递，消除了独立的 GNN 模块，实现模态深度融合。
*   **任务与数据灵活性**：生成式提示范式使得无需替换输出头或重新训练即可在节点分类和链接预测间切换，并天然支持开放词汇的跨数据集迁移。
*   **理论与实践的桥接**：严格证明了拓扑掩码注意力与图消息传递的等价性，为设计提供了理论依据。
*   **简单且有效**：仅通过一个二值注意力掩码注入拓扑，结合 LoRA 微调冻结的 LLM 骨干，在没有引入复杂图编码器或额外预处理的情况下取得先进性能。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
*   **应用限制**：当前框架主要面向静态同质图，未建模动态图演化或异构边（关系类型）的语义差异。
*   **表达力局限**：星形掩码限制了邻居间通信，虽然对目标任务有效，但在需要邻居间精细交互的场景下可能次优。理论等价性表明，TAG-DLM 等价于“掩码诱导图”上的消息传递，而非原始图结构。
*   **可扩展性**：线性化序列长度随邻域大小线性增长，算力开销与序列长度相关。尽管 8B 模型在论文任务上可行，拓展到更大规模图（更多节点、更广邻域）需要更长的上下文窗口和更高计算成本。
*   **评估域与评估方式**：仅在学术引文网络基准上进行，未覆盖其他类型 TAG（如社交网络、知识图谱）。实验种子数（种子 0-2）有限，可能影响稳健性评估。

（完）
