---
title: "Beyond the Golden Teacher: Enhancing Graph Learning through LLM-GNN Co-teaching"
title_zh: 超越黄金教师：通过LLM-GNN协同教学增强图学习
authors: "Zhuoyi Peng, Hanlin Gu, Lixin Fan, Yi Yang"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11583v1"
tags: ["query:graph-llm"]
score: 10.0
evidence: 提出LLM与GNN共教学，用于文本属性图的少样本学习。
tldr: "在文本属性图少样本学习中，GNN与LLM因各自缺陷难以独当一面，现有方法依赖单一黄金教师假设导致监督偏差。本文提出LLM-GNN Co-Teaching双向互教策略，双方交换高置信伪标签，并通过跨轮伪标签转变构造偏好对进行DPO优化。在六个数据集上，3-shot精度平均提升7%以上，并成功扩展到零样本迁移。该方法突破了传统主从式学习范式，增强了模型在困难样本上的表现。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11583v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 434, \"height\": 277, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11583v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 330, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11583v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 678, \"height\": 321, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11583v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1448, \"height\": 288, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11583v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 711, \"height\": 495, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11583v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1442, \"height\": 404, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11583v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1131, \"height\": 746, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11583v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1397, \"height\": 458, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11583v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1340, \"height\": 1485, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11583v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1200, \"height\": 1715, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11583v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1144, \"height\": 405, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11583v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 597, \"height\": 527, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11583v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1299, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11583v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1374, \"height\": 371, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11583v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1391, \"height\": 1367, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11583v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1260, \"height\": 329, \"label\": \"Table\"}]"
motivation: 少样本场景下，固定教师模型会引入其特定盲点，阻碍图与文本的协同学习。
method: 提出LLM-GNN Co-Teaching，双方基于小损失交换高置信伪标签，并利用跨轮预测一致性变化构造偏好对进行DPO优化。
result: "在六个基准上，3-shot平均提升超7%，并成功实现零样本跨数据集迁移。"
conclusion: 放弃固定教师假设，通过双向互教与轨迹监督，显著增强了模型在挑战性样本上的图学习能力。
---

## 摘要
文本属性图(TAGs)构成了引用网络、社交媒体和电子商务等现实应用的基础。在TAGs上进行小样本图学习是困难的：每个类别仅有少量标签，而图的其余部分未标注，无论是图神经网络(GNN)还是大语言模型(LLM)都难以单独学好。GNN读取拓扑结构，但在冷启动节点上失败；LLM读取文本，但在文本歧义节点上失败。现有的LLM-GNN方法都遵循相同的范式：指定一个模型作为黄金教师，并使用其输出（例如特征或伪标签）来监督另一个模型。我们认为，这种黄金教师假设在稀疏监督下会失效：两个模型都不是黄金标准，将任何一个当作黄金教师都会将其盲点传递给学生。因此我们提出：能否避免将任一模型指定为黄金教师，同时仍能进行有效的图学习？我们以LLM-GNN协同教学作为答案，这是一个双向协同教学框架，其中两个模型都不固定为教师。GNN和LLM根据特定架构的小损失准则交换它们最自信的伪标签，并且每一轮都进行更新。然后从轨迹中挖掘监督信号：每当一个节点从第t轮的跨模型矛盾变为第t+1轮的跨模型一致时，LLM对同一输入的两个答案形成一个偏好对（旧的矛盾自我 < 新的同伴认可的自我），用于DPO训练。我们称之为基于轮次的伪标签偏好优化(RPL-PO)。在六个基准测试上，LLM-GNN协同教学持续优于GNN-as-Judge及所有先前方法，在Cora上3-shot绝对提升7.86%，在ogbn-arxiv上提升7.73%；这些改进延续到5-shot和零样本跨数据集迁移。错误结构分析进一步表明，放弃黄金教师假设显著提高了LLM在困难样本上的图学习能力。

## Abstract
Text-attributed graphs (TAGs) underlie real-world applications such as citation networks, social media, and e-commerce. Few-shot graph learning on TAGs is hard: with only a handful of labels per class and the rest of the graph unannotated, neither GNNs nor LLMs can learn well on their own. GNNs read topology and fail on cold nodes; LLMs read text and fail on text-ambiguous nodes. Existing LLM-GNN methods all follow the same recipe: designate one model as the golden teacher and use its outputs (e.g., features or pseudo-labels) to supervise the other. We argue this golden-teacher assumption breaks under sparse supervision: neither model is golden, and treating either as such transfers its blind spots into the student. We therefore ask: can we avoid designating either model as the golden teacher, and still perform effective graph learning? We answer with LLM-GNN Co-Teaching, a bidirectional co-teaching framework in which neither model is fixed as teacher. The GNN and LLM exchange their most confident pseudo-labels under an architecture-specific small-loss criterion, and both update every round. Supervision is then mined from the trajectory: whenever a node moves from cross-model contradiction at round t to cross-model agreement at round t+1, the LLM's two answers on the same input form a preference pair (old contradicting self < new peer-endorsed self) for DPO training. We call this Round-based Pseudo-Label Preference Optimization (RPL-PO). On six benchmarks, LLM-GNN Co-Teaching consistently outperforms GNN-as-Judge and all prior methods, with absolute 3-shot gains of 7.86% on Cora and 7.73% on ogbn-arxiv; improvements carry over to 5-shot and to zero-shot cross-dataset transfer. Error-structure analysis further shows that abandoning the golden-teacher assumption substantially improves the LLM's graph learning capability on challenging samples.

---

## 论文详细总结（自动生成）

好的，请查收基于论文《Beyond the Golden Teacher: Enhancing Graph Learning through LLM-GNN Co-teaching》的结构化总结。

### 1. 论文的核心问题与整体含义

*   **研究动机与背景：**
    *   文本属性图（TAGs）广泛应用于引用网络、社交媒体等领域，但在现实场景中，往往只有极少量的标签（少样本学习），使得图学习极具挑战性。
    *   在少样本场景下，GNN和LLM各自存在明显短板：GNN依赖拓扑结构，对邻居少的“冷节点”表现不佳；LLM依赖文本，对内容简短或歧义性强的节点容易出错。
*   **核心问题：**
    *   现有LLM-GNN方法均遵循“黄金教师假设”，即指定GNN或LLM中的一方作为固定、权威的教师，其输出被当作真值来监督另一方。在标签稀疏时，任何一方都不可靠，扮演教师会将自己的“盲点”错误地传递给学生，限制了整体性能。
    *   因此，论文的核心问题是：**能否打破黄金教师假设，在不指定任何一方为权威的前提下，通过让两者互相学习、共同进化，来实现更有效的图学习？**

### 2. 论文提出的方法论

论文提出名为 **LLM-GNN Co-Teaching (LLM-GNN 协同教学)** 的框架，核心思想是让GNN和LLM两大模型互为师生，通过迭代的方式共同进步，并从一个新颖的视角挖掘监督信号。

*   **核心技术思想：双向协同教学与轨迹挖掘**
    *   **去中心化迭代**：没有固定的教师。每一轮，双方都作为“学生”接收对方的“教学”（伪标签），同时作为“教师”向对方提供自己最自信的预测。双方模型在每轮后都进行更新。
    *   **轨迹偏好挖掘**：利用模型在多轮学习中的预测变化轨迹来构造监督信号。当LLM对一个节点的预测，从“上轮与GNN矛盾”变为“本轮与GNN一致”时，这两个预测结果构成一个天然的偏好对（“新、被认可的回答”优于“旧、矛盾的答案”）。

*   **关键技术细节：三轮驱动的框架流程**
    1.  **选择与交换自信伪标签 (§4.1)**：
        *   在同批未标注数据上，GNN和LLM分别进行预测。
        *   模型根据自身架构特定的“小损失准则”评估预测的自信度：
            *   **GNN**：计算其对该预测类别的交叉熵损失，损失越低越自信。
            *   **LLM**：生成答案序列中所有token的最小对数概率，该值越高越自信。
        *   双方仅保留当前最自信的、比例从R_min到R_max线性递增的伪标签，并交换给对方作为训练真值。
    2.  **更新LLM和GNN (§4.2)**：
        *   **LLM更新**：基于“少量真值标签”和“GNN提供的自信伪标签”，进行监督微调 (SFT)。
        *   **GNN更新**：基于“少量真值标签”和“LLM提供的自信伪标签”进行训练，并采用动态权重α_t来平衡真值损失与伪标签损失，防止伪标签量过大淹没真值信号（α_t从低到高线性增加）。
    3.  **强化伪标签质量：RPL-PO (§4.3)**：
        *   **RPL-PO (Round-based Pseudo-Label Preference Optimization)**：一个独立于SFT的、从学习轨迹中生成偏好对的优化步骤，每两轮执行一次。
        *   **生成偏好对**：对比同一节点在**奇数轮 (t-1)** 和**偶数轮 (t)** 的LLM预测结果。如果满足：(i) 奇数轮时，LLM与GNN预测矛盾；(ii) 偶数轮时，LLM改变预测并与GNN一致。那么，将偶数轮的预测作为“优选 (chosen)”，奇数轮的作为“拒绝 (rejected)”。
        *   **优化目标**：利用生成的偏好对，对LLM进行直接偏好优化 (DPO)，奖励信号完全来自模型自身的进化轨迹，无需任何人工标注或外部奖励模型。

### 3. 实验设计

*   **数据集：**
    *   六个涵盖多领域的TAG基准数据集：
        *   学术引用网络：**Cora**, **Citeseer**, **PubMed**, **ogbn-arxiv**（细粒度学科分类）
        *   百科超链接网络：**WikiCS**
        *   电商共同购买网络：**ogbn-products**（商品分类）

*   **Benchmark与对比方法：**
    *   将提出的 **LLM-GNN Co-Teaching** 与三大类基线方法进行全面对比：
        *   **经典GNN模型**：GCN, GAT, GraphSAGE。
        *   **LLM作为预测器 (LLM-as-Predictor)**：零样本提示(Zero-Shot)、思维链提示(Graph-CoT)、邻居增强提示。
        *   **LLM-图结合法 (LLM-Graph)**：GLEM, TAPE (LLM增强GNN)，LLM-GNN, LLaGA, GraphGPT (GNN增强LLM)，以及最直接的竞争方法 **GNN-as-Judge** (LLM作为预测器，GNN作为固定评判者)。

*   **实验场景：**
    *   **主要场景**：在**3-shot**、**5-shot** 和 **10-shot** 三种小样本半监督设置下进行节点分类。
    *   **零样本迁移**：在ogbn-arxiv上训练，直接评估在Cora、Citeseer、PubMed上的分类准确率，检验模型泛化能力。

### 4. 资源与算力

*   **未明确总计算量**：论文没有明确报告整个项目消耗的总GPU时/卡数，但在附录的“局限性”和“时间分析”部分有较详细的单任务成本说明。
*   **单任务算力与时间 (以ogbn-arxiv 3-shot为例)**：
    *   **GPU**：一张 NVIDIA A100-40GB GPU。
    *   **总耗时**：完成默认10轮协同教学的端到端训练大约需要 **282分钟（约4小时42分钟）**。
    *   **耗时分解**：初始化约17分钟；每轮约26分钟（包含LLM SFT、批量推理、GNN重新训练）；偶数轮的DPO更新额外增加约5分钟。
    *   **对比**：论文明确指出，尽管其计算成本高于GNN-as-Judge（约为其2.6倍），但处于TAPE、GraphGPT等其他高性能LLM-GNN方法的同一成本区间内，并换取了最高的精度。

### 5. 实验数量与充分性

论文进行了**非常全面和充分**的实验验证，设计客观公平。

*   **主实验规模**：覆盖**6个数据集 × 3种小样本设置 (3/5/10-shot)**，提供了完整的性能评估。
*   **消融实验**：在3-shot设置下，对**协同教学结构**（移除双向性、RPL-PO）、**伪标签选择机制**（固定比例、一致性选择）和**训练配置**（移除邻居信息）等关键组件进行了详尽的消融分析。
*   **深度分析实验**：
    *   **错误结构分析**：按节点度数可视化错误分布，揭示了GNN/LLM互补的失败模式及协同教学如何弥补。
    *   **伪标签质量分析**：跟踪了每一轮中双方交换的伪标签精度的上升轨迹。
    *   **鲁棒性与敏感性检查**：更换了不同的GNN主干（GAT, GraphSAGE）、LLM主干（Vicuna-7B）及关键超参数（批量大小、DPO轮数）。
*   **扩展实验**：包含零样本跨数据集迁移能力测试。

### 6. 论文的主要结论与发现

*   **性能最优 (SOTA)**：LLM-GNN Co-Teaching在所有六个数据集的3-shot、5-shot和10-shot任务中，均取得了最高精度，全面超越了包括GNN-as-Judge在内的所有先前方法。
*   **显著提升**：在3-shot设置下，相比最强基线方法GNN-as-Judge，平均绝对提升高达**7.73%** (ogbn-arxiv) 和 **7.86%** (Cora)。增益在小样本下最明显，证明了方法的价值。
*   **消融实验证实各个组件的必要性**：双向教学结构、从小到大的递进式伪标签筛选、以及利用学习轨迹的RPL-PO，都对最终性能有重大贡献，移除任何一项都会导致性能下降。
*   **机制有效性验证**：错误结构分析证实了GNN（依赖度低）和LLM（文本歧义）失败的互补性，而协同教学成功缩小了这两个盲区的错误，使双方都得到提升。
*   **泛化能力**：协同教学学到的能力可以零样本迁移到其他数据集，表现出比单轮方法更强的通用图推理能力。

### 7. 优点

*   **范式创新**：勇敢地挑战并打破了领域内根深蒂固的“黄金教师”假设，提出的双向互教范式更为合理，为GNN和LLM的结合提供了新思路。
*   **监督信号挖掘方式巧妙**：**RPL-PO** 是该方法的一大亮点。它巧妙地利用了模型在多轮互教中的“预测状态转变”作为免费的、自监督的偏好信号，构思新颖且无需额外成本。
*   **实验验证极其扎实**：实验设计涵盖了多个维度，不仅证明了效果好，还通过详尽的错误分析、鲁棒性测试和消融研究，深刻解释了**为什么好**，使结论极具说服力。
*   **性能提升巨大**：在极具挑战性的小样本场景下，性能指标的大幅提升，证明了该方法的实用价值。

### 8. 不足与局限

*   **时间与计算成本较高**：迭代式训练和每轮对LLM的推理/微调，使得计算开销是单轮方法的数倍（论文自身也指出了这一点）。
*   **数据域覆盖有限**：实验主要在文本描述性较强的图（学术、商品）上进行，尚未在文本稀疏、噪声大或缺失的图（如分子图、交易网络）上验证，在这些场景下模型的互补性假设可能不再成立。
*   **LLM依赖性强**：鲁棒性实验表明，框架的性能严重受限于LLM基座的能力，弱LLM会导致显著的性能下降。
*   **潜在的偏差放大风险**：LLM本身携带的预训练语料偏差，可能在互教循环中被GNN修正不足而持续放大，尤其是在GNN也难以处理的样本上。
*   **超参数配置不变性未充分探究**：论文虽展示了对手动调整的线性R(t)和αt的优势，但未探索这些关键调度策略的其他形式（如余弦退火），或许存在更优配置。

（完）
