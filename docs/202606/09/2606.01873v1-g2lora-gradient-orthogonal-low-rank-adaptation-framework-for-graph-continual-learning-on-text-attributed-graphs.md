---
title: "G2LoRA: Gradient Orthogonal Low-Rank Adaptation Framework for Graph Continual Learning on Text-Attributed Graphs"
title_zh: G2LoRA：面向文本属性图的梯度正交低秩适应图持续学习框架
authors: "Yuhan Wang, Yibo Ding, Yutong Ye, Mufan Zhao, Wenbo Zhang, Ruijie Wang, Jianxin Li"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.01873v1"
tags: ["query:graph-llm"]
score: 9.0
evidence: LLM-as-Aligner预训练对齐文本属性图的图和文本模态
tldr: 针对文本属性图上LLM-as-Aligner模型顺序微调时的灾难性遗忘与任务干扰问题，本文提出G2LoRA框架。该框架统一多层级任务的图-文本对齐目标，并采用类别感知梯度投影在结构化子空间中缓解冲突更新，促进正向迁移；同时通过梯度幅度调制协调图与文本编码器的更新速率，防止跨模态漂移。在基准数据集上的实验表明，G2LoRA在各种增量模式下均取得优越的持续学习性能，显著超越现有基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01873v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 878, \"height\": 746, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01873v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1726, \"height\": 729, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01873v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 693, \"height\": 664, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01873v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 844, \"height\": 796, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01873v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 871, \"height\": 346, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.01873v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 883, \"height\": 294, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.01873v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 844, \"height\": 341, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01873v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1840, \"height\": 875, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01873v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 580, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01873v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 909, \"height\": 177, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01873v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1835, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01873v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 878, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01873v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 878, \"height\": 209, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.01873v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 883, \"height\": 631, \"label\": \"Table\"}]"
motivation: 在文本属性图上顺序微调LLM-as-Aligner模型时，异构下游任务和图-文编码器更新敏感度差异导致严重遗忘与不对齐。
method: 提出G2LoRA框架：统一多任务对齐目标，利用类别感知梯度投影化解冲突，并调制梯度幅度协调编码器更新。
result: 在多个基准数据集和增量模式下，G2LoRA持续性能大幅超越现有参数高效微调方法，实现更好的任务迁移。
conclusion: G2LoRA有效缓解TAG持续学习中的干扰与漂移，促进了跨任务知识迁移，为图-文模型在线适应提供新路径。
---

## 摘要
LLM-as-Aligner 已成为文本属性图（TAGs）的主流预训练范式，通过 CLIP 风格的对比学习将图与文本模态对齐到共享嵌入空间。虽然在单个下游任务上有效，但我们观察到当此类模型在流式任务上顺序微调时会出现严重的灾难性遗忘。尽管参数高效微调在一定程度上缓解了遗忘，但仍不足以解决任务干扰和无效知识迁移。本文研究 TAGs 上 LLM-as-Aligner 模型的图持续学习，目标是减轻干扰并促进任务间的正向迁移。该设定带来两个根本挑战：（1）异构的下游任务导致优化目标漂移，阻碍统一微调；（2）图编码器和文本编码器对适配表现出不同敏感性，导致更新不协调容易造成错位。为应对这些挑战，我们提出 G2LoRA，一个面向 TAGs 的持续学习框架。G2LoRA 将节点级、链路级和图级任务统一在单个图-文本对齐目标下，并支持跨域/类/任务增量模式的一致优化。为减少任务干扰同时鼓励正向迁移，G2LoRA 在结构化子空间中执行类别感知的梯度投影，解决冲突更新并支持有条件的反向迁移以平衡前向与后向知识流。为进一步防止跨模态漂移，G2LoRA 引入梯度幅度调制来协调图编码器和文本编码器之间的更新速率。在基准数据集上的大量实验表明，G2LoRA 在不同骨干架构上始终优于强基线，实现了卓越的持续性能和可迁移性。

## Abstract
LLM-as-Aligner has emerged as a prevalent pre-training paradigm for Text-Attributed Graphs(TAGS), aligning graph and text modalities into a shared embedding space via CLIP-style contrastive learning. While effective on individual downstream tasks, we observe severe catastrophic forgetting when such models are sequentially fine-tuned on streaming tasks. Although parameter-efficient fine-tuning alleviates forgetting to some extent, it remains insufficient to resolve task interference and ineffective knowledge transfer. In this work, we study graph continual learning for LLM-as-Aligner models on TAGs, with the goal of mitigating interference while promoting positive transfer across tasks. This setting introduces two fundamental challenges: (1) heterogeneous downstream tasks induce shifting optimization objectives, hindering unified fine-tuning; and (2) graph and text encoders exhibit different sensitivities to adaptation, making uncoordinated updates prone to misalignment. To address these challenges, we propose G2LoRA, a continual learning framework for TAGs. G2LoRA unifies node-, link-, and graph-level tasks under a single graph--text alignment objective, and enables consistent optimization across domain/class/task incremental modes. To reduce task interference while encouraging positive transfer, G2LoRA performs category-aware gradient projection in structured subspaces, resolving conflicting updates and enabling conditional backward transfer to balance forward and backward knowledge flow. To further prevent cross-modal drift, G2LoRA introduces gradient magnitude modulation to coordinate update rates between graph and text encoders. Extensive experiments on benchmark datasets demonstrate that G2LoRA consistently outperforms strong baselines across different backbone architectures, achieving superior continual performance and transferability.

---

## 论文详细总结（自动生成）

好的，以下是基于提供的论文内容生成的结构化中文总结。

### 1. 论文的核心问题与整体含义

*   **研究背景与动机**
    *   文本属性图（TAGs）在现实应用中非常普遍，LLM-as-Aligner 范式通过对比学习对齐图与文本模态，在下游任务中表现优异。
    *   **核心问题**：本研究观察到，将 LLM-as-Aligner 模型顺序微调到流式任务时，会发生**严重的灾难性遗忘**。即便采用参数高效微调（PEFT，如 LoRA），**任务干扰**和**无效知识迁移**问题依然存在。
    *   **研究目标**：旨在探索 LLM-as-Aligner 模型的图持续学习，核心目标是**减轻任务间干扰，同时促进任务间的正向知识迁移**。

*   **两个根本性挑战**
    1.  **异构下游任务导致优化目标漂移**：节点级、链路级和图级任务目标不同，无法用单一的微调策略有效应对，且任务间关系（如类别相似度）在不同增量场景下呈现复杂模式。
    2.  **跨模态敏感度不平衡导致对齐漂移**：图编码器与文本编码器结构不同，对微调信号的响应速度不一致，导致更新量级失衡，进而扭曲共享嵌入空间，破坏图-文对齐。

### 2. 论文提出的方法论

*   **核心思想**：提出 **G²LoRA** 框架，通过统一任务目标、类别感知的梯度投影和跨模态梯度调制，在参数高效的条件下实现鲁棒的图持续学习。

*   **关键技术细节**
    1.  **统一任务公式化**
        *   将节点级、链路级和图级任务统一转化为**图-文本对齐目标**。通过计算不同粒度的图嵌入与文本类别嵌入的余弦相似度矩阵 \(\Lambda\)，并使用 CLIP 风格的双向对比损失 \(L(\Lambda)\) 进行优化，使得所有下游任务能共享一个一致的优化框架。
    2.  **类别感知双向梯度投影 (CBGP)**
        *   **类别感知梯度子空间**：将历史任务的梯度按类别划分，通过 SVD 分解为类别判别子空间，构成历史梯度联合子空间 \(M_{1:t}\)，从而在基础的类别空间统一建模任务冲突与迁移。
        *   **动态双向迁移建模**：
            *   **前向更新（抗干扰）**：将当前任务的梯度投影到历史子空间的正交补空间上 \((H_t \cap M^\perp_{1:t-1})\) 来初始化 LoRA 下投影矩阵 \(B_t\)，避免干扰旧知识。
            *   **后向更新（促迁移）**：检测当前梯度与历史任务共享子空间 \((M_{1:i-1} \cap M_i)\) 的对齐条件，若满足阈值 \(\epsilon_1\) 则激活后向迁移，将当前梯度在该共享空间上的投影分量用于更新历史任务的 LoRA 分支，以巩固知识并实现正向迁移。论文提供了定理证明双向更新能带来更低损失。
    3.  **梯度调制机制**
        *   通过监测图编码器和文本编码器梯度范数的比值（学习速率比 \(\rho_t\)），动态地调整学习速率。对学习过快的模态施加衰减（如 \(\eta_t = \eta_0 \cdot \lambda \cdot \rho_t\)），确保两个编码器以协调的速度更新，避免跨模态表征漂移。

*   **算法流程**
    *   G²LoRA 冻结预训练的双编码器骨架，仅微调轻量级的 LoRA 适配器。面对顺序到来的任务，依次执行：统一任务公式化、类别感知前向投影以初始化新任务的 LoRA 分支、条件判断并执行后向投影以更新旧分支、计算模态学习速率比并动态调制两个编码器的学习率。

### 3. 实验设计

*   **数据集与场景**
    *   **预训练**：在 5 个大规模 TAG 数据集（ArXiv-2023, OGBN-ArXiv 等）上预训练。
    *   **下游持续学习评估**：在 6 个小规模 TAG 数据集（Cora, Citeseer, WikiCS, Photo, Computer, History）上组织任务，覆盖三种增量学习场景：
        *   **CIL (Class-Incremental Learning)**：**类增量**，在 Photo, Computer, History 数据集上进行。
        *   **DIL (Domain-Incremental Learning)**：**域增量**，组合 Cora+Citeseer+WikiCS 形成一个多域任务序列。
        *   **TIL (Task-Incremental Learning)**：**任务增量**，混合节点级 (Cora)、链路级 (WikiCS) 和图级 (Photo) 任务。
    *   **设定**：严格的 **k-shot 学习** (k=10) 和流式数据，历史数据不可见。

*   **基准对比方法**
    *   共对比了 **18种** 基线方法，分为三大类：
        *   **GNN-based**: GCN, EWC, LwF, Cosine, TPP, TEEN。
        *   **LLM-based**: BERT, RoBERTa, LLaMA, SimpleCIL。
        *   **GLM-based**: GCNEmb, GraphPrompter, GraphGPT, LLaGA, SimGCL（以及LLM-as-Aligner 基线 GraphCLIP, G2P2）。同时对比了 LoRA、InfLoRA、SD-LoRA 等 PEFT 方法。

*   **评价指标**
    *   **AA (Average Accuracy)**：所有任务的性能均值，衡量整体学习效果。
    *   **AF (Average Forgetting)**：历史任务性能的退化程度，衡量抗遗忘能力（值越高忘记越少）。

### 4. 资源与算力

*   **训练环境**：所有模型使用 PyTorch 实现，并在 **NVIDIA A100-80G GPU** 上训练。
*   **算力细节**：预训练阶段使用单张 A100-80G GPU，训练 30 个周期，批次大小为 800。下游微调阶段批次大小为 32。具体的总训练时长或 GPU 使用数量，文中未明确给出。

### 5. 实验数量与充分性

*   **实验数量庞大，设计充分**：
    *   **主实验**：在 3 种增量场景、多组数据集组合下，与 18 种基线方法进行性能对比（AA/AF），每个实验重复 5 次。
    *   **消融研究**：逐步移除 G²LoRA 的四个核心组件（条件后向迁移、梯度调制、双向投影、类别感知）来验证各自的有效性。
    *   **效率分析**：对比了 G²LoRA 与标准 LoRA 在性能与训练时间上的权衡。
    *   **机制有效性验证**：专门设计实验，通过分析参数变化方差和遗忘率，证明了梯度调制机制在平衡跨模态更新中的作用。
    *   **泛化性验证**：将 G²LoRA 集成到不同的 LLM-as-Aligner 模型（G2P2, GraphCLIP）上进行测试。
    *   **少样本鲁棒性测试**：在 1-shot, 3-shot, 5-shot, 10-shot 设定下评估性能。
    *   **超参数敏感性分析**：分析了关键超参数（\(\epsilon_1, \lambda\)）在不同增量场景下的影响。
*   **公平性**：实验严格遵循不访问未来数据的顺序学习范式，对比方法均使用统一的语言模型主干（LLaMA-3-8b 等），优化器和训练协议保持一致，确保了对比的公平性。

### 6. 论文的主要结论与发现

*   G²LoRA 在 CIL、DIL、TIL 所有三种增量学习场景下，一致地且大幅超越了所有 18 个对比基线，取得了 **SOTA 性能**，平均相对提升达到 **7.59%**。
*   在部分数据集上，G²LoRA 的 AF（平均遗忘率）出现了**正向遗忘率**，表明不仅没有遗忘，反而通过反向迁移提升了在旧任务上的表现。
*   统一任务公式化对于处理台任务增量学习中的异构目标至关重要。
*   类别感知梯度投影和双向迁移机制是平衡模型稳定性（抗遗忘）与可塑性（学新知）的关键。
*   梯度调制机制有效地协调了图与文本编码器的优化，防止了模态漂移。
*   该框架具备良好的**泛化性和鲁棒性**，能适配不同的对齐模型骨干，并在极低资源（如1-shot）下保持优势。

### 7. 优点

*   **问题新颖性**：首次将图持续学习与 LLM-as-Aligner 范式相结合，精准地指出了两大挑战（任务异构与模态漂移）。
*   **方法创新性强**：提出的类别感知梯度子空间和动态双向迁移机制，从更细致的类别层级解决了任务冲突和迁移问题，具有坚实的理论（定理）支撑。统一的任务公式化也是一种简洁有效的设计。
*   **性能卓越**：在各项指标上均取得显著提升，特别是“正向遗忘”现象，优于单纯抗遗忘的方法。
*   **实验扎实全面**：覆盖多种场景、大量的对比方法、详尽的消融与多维度分析实验，论证过程严谨且具说服力。

### 8. 不足与局限

*   **任务粒度与数据漂移**：G²LoRA 主要面向离散的任务序列（Class/Domain/Task Incremental）。对于数据分布随时间的渐变式漂移（Concept Drift），而非明确的任务边界变更，其适应性未做探讨。
*   **计算与存储开销**：CBGP 模块需要为每个历史类别维护梯度子空间（通过 SVD 计算），随着任务和类别的增加，虽然缓解了遗忘，但其内存和计算开销会线性增长，可能成为大规模流数据场景下的瓶颈。
*   **后向迁移的风险**：条件后向迁移机制虽然有效，但依赖于阈值 \(\epsilon_1\) 的设定。文中实验表明该阈值在不同增量场景下最优值不同，且对 TIL/DIL 更敏感（较小阈值），这可能在实际部署中需要额外的调优。
*   **应用范围限制**：该方法特定于 LLM-as-Aligner 架构（双编码器）的图持续学习，对于其他类型的 TAG 学习方法（如 LLM-as-Predictor）的适用性未经验证。
*   **任务ID假设**：CIL 场景下的类别过滤模块依赖于预测任务ID来选取原型，这要求模型能够判定样本来自哪个任务，而在无任务ID提示的“任务不可知”（task-agnostic）CIL场景下可能受限。

（完）
