---
title: "SVGym (SciVerseGym): An Environment for Reinforcement Learning and Bayesian Optimization in Crystal Discovery"
title_zh: "SVGym (SciVerseGym): 一种用于晶体发现中强化学习与贝叶斯优化的环境"
authors: Bin Cao
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22425v1"
tags: ["query:ml"]
score: 8.0
evidence: 提供通过强化学习和贝叶斯优化进行自动化晶体发现的强化学习环境
tldr: 当前闭环晶体搜索方法碎片化，缺乏统一交互接口。SciVerseGym将其建模为马尔可夫决策过程，提供Gymnasium兼容环境，包含元素替换、晶格扰动、空位创建等编辑操作，并以机器学习势能进行评估。该环境解耦智能体与材料基础设施，为强化学习、贝叶斯优化等提供开放测试基准，促进算法复现与公平比较，有望加速自动化晶体发现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22425v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1605, \"height\": 1447, \"label\": \"Figure\"}]"
motivation: 现有闭环晶体搜索流程碎片化，缺乏标准化的序列决策测试环境。
method: 将晶体发现构建为马尔可夫决策过程，提供Gymnasium兼容环境，支持局部与全局原子编辑及可配置评估。
result: 构建了SciVerseGym开放测试平台，可用于强化学习、贝叶斯优化等算法的复现与比较。
conclusion: SciVerseGym为闭环晶体发现提供了统一评估框架，推动材料加速发现。
---

## 摘要
机器学习原子间势能现已能实现高效的原子级评估，以支持交互式材料发现，然而闭环晶体搜索方法仍分散于各自定制的编辑、弛豫、评分、约束和记录流程中。我们提出了 SciVerseGym，这是一个与 Gymnasium 兼容的环境，用于序列晶体发现，将晶体设计建模为马尔可夫决策过程。智能体观察原子结构，应用化学上合理的编辑，并从可配置的评估器接收反馈。SciVerseGym 支持局部和全局动作，包括元素替换、晶格扰动、原子位移、空位创建和原子插入，以及可配置的化学空间、结构池、基于原子和图结构的观测、自定义奖励、可选的弛豫，以及稳定性或声子相关诊断。每一步应用一次编辑，使用机器学习原子间势能或任何 ASE 兼容的计算器评估候选结构，并返回标准的 (obs, reward, terminated, truncated, info) 元组。通过将智能体逻辑与材料基础设施解耦，SciVerseGym 为强化学习、贝叶斯优化、进化搜索和语言智能体工作流在闭环晶体发现中提供了一个开放、可复现且可扩展的测试平台。代码见：https://github.com/Bin-Cao/SciVerseGym。

## Abstract
Machine-learned interatomic potentials now enable efficient atomistic evaluation for interactive materials discovery, yet closed-loop crystal search methods remain fragmented across bespoke pipelines for editing, relaxation, scoring, constraints, and bookkeeping. We introduce SciVerseGym, a Gymnasium-compatible environment for sequential crystal discovery that frames crystal design as a Markov decision process. Agents observe an atomistic structure, apply chemically meaningful edits, and receive feedback from a configurable evaluator. SciVerseGym supports local and global actions, including elemental substitution, lattice perturbation, atomic displacement, vacancy creation, and atom insertion, along with configurable chemical spaces, structure pools, atomistic and graph-based observations, custom rewards, optional relaxation, and stability or phonon-related diagnostics. Each step applies an edit, evaluates the candidate using a machine-learned interatomic potential or any ASE-compatible calculator, and returns the standard (obs, reward, terminated, truncated, info) tuple. By decoupling agent logic from materials infrastructure, SciVerseGym provides an open, reproducible, and extensible testbed for reinforcement learning, Bayesian optimization, evolutionary search, and language-agent workflows in closed-loop crystal discovery. Code is available at: https://github.com/Bin-Cao/SciVerseGym.

---

## 论文详细总结（自动生成）

## 1. 论文核心问题与整体含义

- **研究背景**：闭环晶体发现（Closed‑loop Crystal Discovery）依赖计算算法迭代地提出候选结构、评估反馈并改进搜索。但现有方案（强化学习、贝叶斯优化、进化算法、语言智能体等）各自编写独立的结构编辑、弛豫、评分、约束处理及实验记录代码，形成碎片化管道，难以公平比较和复现。
- **核心问题**：缺乏一个标准化的、与 Gymnasium 兼容的交互环境，使得不同搜索算法能在统一的原子尺度评估和反馈协议下进行对比与开发。
- **整体含义**：作者提出 SciVerseGym，一个将晶体发现建模为马尔可夫决策过程（MDP）的开放测试平台，旨在解耦智能体逻辑与材料科学基础设施，从而为闭环材料搜索提供一个可重现、可扩展的公共基准。

## 2. 方法论

### 2.1 核心设计思想

- **结构化编辑而非一次性生成**：智能体通过预定义的晶体编辑动作（元素替换、晶格扰动、原子位移、空位创建、原子插入）交互，而不是直接输出完整的晶体文件。这降低了无效输出，并统一了不同算法的接口。
- **标准闭环转移接口**：遵循 Gymnasium 的 `(obs, reward, terminated, truncated, info)` 协议，每一步返回转移元组，使任意依赖状态‑动作‑反馈的算法均可无缝接入。
- **搜索策略与物理评估分离**：评估后端（如 SevenNet、MatterSim、ORB 等 MLIP，或任何 ASE 兼容计算器）在环境初始化时指定，而智能体逻辑不受特定后端限制。
- **透明且可扩展的反馈**：信息字典（`info`）提供形成能、凸包能量距离、声子稳定性等诊断量，奖励仅作为标量目标，便于调试和自定义目标。

### 2.2 关键技术细节

- **动作空间**：五类编辑动作以字典形式编码，包含动作类型、目标原子索引、元素、位移矢量或分数坐标，并内置有效性约束（如禁止清空结构、限制位移幅度）。
- **观测构建**：支持原始 ASE 原子对象、图表示、能量及步数/预算等书本变量。
- **评估与诊断**：
  - 使用 MLIP 直接获取总能量和力，并可组合参考数据计算形成能：  
    \(E_{\text{form}} = \frac{E - \sum_i n_i \mu_i}{N}\)
  - 能量距凸包（Energy above hull）基于相同力场下的参考相计算；声子稳定性通过 Phonopy 的有限位移法可选诊断。
- **默认奖励函数**：  
  \(r = 0.80\,r_{\text{stability}} + 0.10\,r_{\text{abundance}} + 0.10\,r_{\text{novelty}}\)  
  分别对应凸包能量距离、平均原子序数（丰度代理）以及循环内未出现过的化学式新颖性。该奖励仅作为透明基准，实际任务可替换。

### 2.3 智能体协议举例

- **贝叶斯优化 (BO)**：将当前状态的候选编辑映射为固定维特征向量 \(\phi(s_t, a)\)，用代理模型求取采集函数（例如 UCB）最大值作为次步动作，并更新历史数据。
- **强化学习 (RL)**：策略 \(\pi_\theta(a|s_t)\) 直接映射状态到编辑动作，通过折扣累积回报训练，可利用图神经网络或语言模型增强状态表达。

## 3. 实验设计

- **数据集**：默认结构池使用 ALEX‑MP‑20 数据集，也支持用户自定义结构集。
- **基准与对比方法**：**论文本身为系统设计论文，未报告具体实验对比**。文中仅描述了 BO 和 RL 智能体协议作为示例，强调环境可支持多种搜索范式，但未给出它们的性能比较、消融实验或在不同后端上的基准结果。
- **场景**：环境本身可配置化学空间、评估后端、奖励函数等，从而构建各种材料发现任务，但暂无公开的标准化任务集。

## 4. 资源与算力

- **算力说明**：全文**未提及**任何 GPU 型号、训练时长、计算开销或基础设施要求。论文作为环境发布，仅标注了代码仓库和依赖项，未给出环境中所需评估后端（MLIP）的典型计算成本分析。

## 5. 实验数量与充分性

- **实验覆盖度**：本文未进行对比实验、消融实验或基准测试，其贡献集中在环境的设计、接口定义与代码实现。
- **充分性评判**：因此无法从实验角度评估充分性。论文的验证仅体现在代码实现、示例 agent 协议和文档中，实际效果的公平性和鲁棒性需在后续社区使用中检验。

## 6. 主要结论与发现

- SciVerseGym 通过统一的 MDP 环境与结构化编辑动作，有效解耦了搜索算法和材料评估基础设施。
- 这一设计使得 RL、BO、进化搜索、蒙特卡罗及语言智能体能够在完全相同的原子评价协议下运行，极大促进了方法的可复现性和公平对比。
- 默认奖励函数提供了透明基准，而丰富的诊断信息有助于深入分析算法行为。

## 7. 优点

- **高度模块化**：动作、观测、评估器、奖励等均可配置，便于快速原型化和领域定制。
- **标准化接口**：深度集成 Gymnasium，降低学习成本，可直接利用现有 RL/BO 工具库。
- **物理反馈丰富**：除了标量奖励，`info` 字典可返回形成能、凸包距离、声子稳定性等信息，支持多目标决策。
- **开放与可扩展**：代码开源，支持自定义计算器和数据集，可集成多种 MLIP（SevenNet, MatterSim, ORB）和参考数据。
- **避免碎片化**：为多种算法提供同一套基本编辑操作和有效性规则，减少了因实现细节引起的性能差异。

## 8. 不足与局限

- **无实验验证**：论文未提供任何对比实验或基准结果，其实际效果和加速比无法从文中得知。
- **物理有效性受限于计算器**：预测的绝对可靠性取决于所选力场和参考数据，且能量距凸包等量只能在同一计算约定下可比，可能导致错误结论。
- **默认奖励过于简单**：丰度和新颖性代理难以覆盖合成可行性、成本或功能性能，需要用户根据任务重设奖励。
- **动作约束有限**：结构化编辑降低了无效输出，但不保证电荷平衡、合成可行等深层化学合理性。
- **诊断成本未评估**：声子计算等高级诊断在默认配置下可选但昂贵，可能影响实用性。

（完）
