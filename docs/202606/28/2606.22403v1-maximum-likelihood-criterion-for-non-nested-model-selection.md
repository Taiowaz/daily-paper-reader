---
title: Maximum Likelihood Criterion for Non-nested Model Selection
title_zh: 非嵌套模型选择的极大似然准则
authors: Min Tsao
date: 2026-06-21
pdf: "https://arxiv.org/pdf/2606.22403v1"
tags: ["query:ml"]
score: 9.0
evidence: 提出非嵌套模型选择的最大似然准则
tldr: 传统模型选择依赖信息论或贝叶斯惩罚准则，但在非嵌套候选模型场景下，惩罚方法可能失效。为此，本文提出最大似然准则，直接选择最大似然值最高的候选模型，不纳入参数数目惩罚。理论证明该准则具有一致性，且与AIC、BIC等经典惩罚准则的对比实验表明其表现更优。该准则适用于所有候选模型视为同等、不偏好少参数模型的场景，为相关领域提供了新的选择工具。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22403v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 970, \"height\": 977, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22403v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1537, \"height\": 999, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.22403v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1093, \"height\": 328, \"label\": \"Table\"}]"
motivation: 非嵌套模型选择中，基于信息论或贝叶斯的惩罚准则可能不适配，甚至产生反效果。
method: 提出最大似然准则，直接以模型的最大似然值为选择依据，不惩罚参数数量。
result: 理论证明该准则一致，且数值比较表明其优于AIC、BIC等经典惩罚准则。
conclusion: 该最大似然准则为所有候选模型视为平等、不偏好少参数模型的场景提供了有效选择方案。
---

## 摘要
惩罚是一种广泛使用的模型选择方法，其根源可追溯到信息论和贝叶斯推断。我们研究了一个涉及非嵌套候选模型的模型选择问题，对于这类问题，惩罚方法会产生反效果。我们针对这种非嵌套情形提出了一种极大似然准则，该准则选择具有最高极大似然的候选模型。此准则不考虑候选模型的参数个数。它非常适用于所有候选模型被视为平等、且不偏好参数较少模型的情况。我们建立了该准则的一致性，并将其性能与现有基于惩罚的准则进行了比较。

## Abstract
Penalization is a widely used approach to model selection with roots in information theory and Bayesian inference. We study a model selection problem involving non-nested candidate models for which penalization is counterproductive. We propose a Maximum Likelihood Criterion for this non-nested setting that selects the candidate model with the highest maximum likelihood. This criterion does not take into consideration the number of parameters of a candidate model. It is well-suited for situations where all candidate models are regarded as equal with no preference for models having fewer parameters. We establish the consistency of this criterion and compare its performance with that of existing penalization-based criteria.

---

## 论文详细总结（自动生成）

### 1. 研究动机与核心问题
- 传统模型选择（如AIC、BIC）依赖对复杂度的惩罚，其根植于信息论或贝叶斯推断，适用于嵌套模型或回归变量选择场景。
- 在**非嵌套候选模型**的设定下，所有模型被视为“同等重要”，不存在“参数越多拟合越好”的嵌套逻辑，因此复杂度惩罚不仅无必要，反而会引入**对参数较多模型的系统性偏差**。
- 本文聚焦于：当真实模型存在且包含在候选集合中，且候选模型相互非嵌套、无偏好时，如何公平、一致地选出真实模型。

### 2. 方法论：极大似然准则
#### 2.1 核心定义
- **MLC（Maximum Likelihood Criterion）** 直接选择**最高最大对数似然**的候选模型：
  \[
  \text{MLC}(X_n) = \arg\max_{M_j} \ell_j(\hat{\theta}_j),
  \]
  其中 \(\hat{\theta}_j\) 是模型 \(M_j\) 的极大似然估计（MLE）。
- **不惩罚参数个数**，仅依据模型对数据的拟合优度。

#### 2.2 理论一致性证明
- 基于**Kullback-Leibler散度**的正性：若真实分布 \(f\) 不在备选族 \(g(\cdot;\theta)\) 中，则最小KL散度 \(\Delta > 0\)。
- 利用一致大数定律和强/弱大数定律，可得：
  \[
  \frac{1}{n}\left[\sum \log f(X_i) - \sum \log g(X_i;\hat{\theta}_n)\right] \xrightarrow{p} \Delta > 0,
  \]
  进而真实模型的对数似然在概率上渐近大于误设模型。
- 推论：在非嵌套两模型情况下，\(\mathbb{P}(\text{MLC 选中真实模型}) \to 1\) 当 \(n\to\infty\)，即**MLC是一致的**。
- 同时指出：任何 \(p(d,n)=o(n)\) 的惩罚类准则（包括AIC、BIC）在此设定下也是一致的，但惩罚项会带来有限样本下的不公。

### 3. 实验设计与对比方法
#### 3.1 模拟实验
- **模型对**（表1）：
  - 指数分布 vs 对数正态分布；
  - 均匀分布 vs 正态分布；
  - 卡方分布 vs 伽马分布（故意违反非嵌套假设，作为对照）。
- 每种配对下，交替设定其中一个为真实模型，固定参数生成数据。
- **样本量**：\(n = 10, 50, 100\)。
- **对比准则**：MLC（即最大对数似然）、AIC、BIC。
- 每次设定下进行 **10,000次独立模拟**，记录错误率（误选另一模型的比率）。

#### 3.2 真实数据实例
- **数据集**：R内置数据集 `precip`，包含美国70个城市的年均降水量。
- **候选模型**：正态、Logistic、Laplace、伽马、卡方（5个非嵌套模型）。
- 给出每个模型的参数数量、对数似然（MLC）、AIC、BIC值，并比较排序。

### 4. 资源与算力
- 文中未提及任何GPU资源、专用硬件或训练时长。所有计算为纯统计模拟与极大似然估计，可在普通CPU上完成，无需特殊算力说明。

### 5. 实验数量与充分性
- **模拟实验**：3组模型对 × 2种真实模型方向 × 3个样本量 = 18种场景，每种10,000次重复，共计**180,000次模拟**；此外还包含一组嵌套情形（卡方 vs 伽马）作为验证。
- **真实数据**：1个数据集，1次直接比较。
- 实验设计充分覆盖了对称比较、小样本与大样本表现、非嵌套与嵌套对照，体现了客观性和公平对比的原则。

### 6. 主要结论与发现
- **一致性成立**：在真实模型属于候选集且非嵌套条件下，MLC能以概率1渐近选出真实模型。
- **有限样本表现**：MLC对参数数目更多的模型**没有系统性偏好或惩罚**，其错误率对称且公平；而AIC和BIC因惩罚项明显偏向参数少的模型，在真实模型参数较多时错误率更高。
- **嵌套情形**：当候选模型嵌套时（如卡方嵌套于伽马），MLC始终选择更大模型（错误率100%），说明MLC不适用于处理过拟合的嵌套选择问题，此时惩罚项才有价值。
- **真实数据**：各准则在 `precip` 数据上给出了一致的排序，支持了在较大样本下准则间差异逐渐消失的结论。
- 最终推荐：在候选模型**地位平等、无嵌套关系且无过拟合忧虑**的情境下，MLC是比AIC/BIC更公平的选择准则。

### 7. 优点与亮点
- **理论严谨**：用KL散度等经典工具严格证明了一致性，并清晰指出了惩罚准则一致的条件及其偏差来源。
- **方法简洁直观**：直接比较最大似然，无需额外调整参数或先验，易于实行。
- **对比全面**：对称地互换真实模型与备选模型，揭示了惩罚项的不对称偏差，论证极具说服力。
- **实用导向**：给出了明确的适用范围（非嵌套、所有模型同等重要），为工程和科学领域（如可靠性工程）提供了清晰指导。

### 8. 不足与局限
- **强假设**：要求真实模型一定在候选集合中，且模型族满足紧致性等正则条件；实际中未必成立。
- **非嵌套依赖性**：若存在部分嵌套或重叠，方法可能失效（如文中所展示的卡方 vs 伽马情形）。
- **忽略模型不确定性**：仅选出一个“最佳”模型，未提供模型概率或平均，无法量化选择不确定性。
- **实验覆盖有限**：仅测试了少数单变量分布对，对更复杂的模型（如回归模型中的非嵌套比较、高维参数）未做探讨。
- **无有限样本理论保障**：一致性是大样本性质，小样本下MLC的优势主要基于模拟，缺乏类似于AIC/BIC的决策理论或信息论解释。
- **未讨论模型误判的后果**：选择错误模型对后续推断的影响未深入分析。

（完）
