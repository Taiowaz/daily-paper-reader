---
title: Guiding Federated Graph Recommendation with LLM-encoded knowledge
title_zh: 利用LLM编码知识引导联邦图推荐
authors: "Thi Minh Chau Nguyen, Hien Trang Nguyen, Duc Anh Nguyen, Van Ho-Long, Thanh Trung Huynh, Zhao Ren"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15277v1"
tags: ["query:graph-llm"]
score: 9.0
evidence: 利用LLM编码知识引导联邦图推荐训练，将GNN与LLM结合
tldr: 联邦图推荐面临客户端数据非独立同分布、结构嵌入不对齐及简单聚合失效的挑战，现有方法仅依赖结构信息，忽略了大语言模型丰富的全局语义。本文提出新框架，客户端利用冻结LLM将局部交互模式编码为语义向量，服务器基于语义发现跨客户端偏好关联，指导结构表示的选择性聚合。在标准基准上，该方法推荐精度持续优于基线，证明融合LLM知识可有效引导联邦图协作学习，提升推荐性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15277v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 766, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15277v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 525, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15277v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1257, \"height\": 560, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15277v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1224, \"height\": 522, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15277v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1230, \"height\": 711, \"label\": \"Table\"}]"
motivation: 现有联邦图推荐忽略大语言模型的全局语义，导致客户端结构嵌入对齐困难，限制推荐性能。
method: 客户端用冻结LLM将局部交互模式编码为语义向量，服务器基于语义相似性指导结构表示的选择性聚合。
result: 实验表明，该方法在多个标准基准上较联邦图基线一致提升推荐准确性。
conclusion: 利用LLM语义知识可有效引导联邦图结构对齐，为隐私保护下的跨客户端协作推荐提供新思路。
---

## 摘要
基于图的推荐系统能高效地从用户-物品交互中提取协同信号，而联邦学习（FL）使得这些模型在训练时能保护用户隐私。然而，在分布式、非独立同分布（non-IID）的客户端间聚合图表征仍是一项挑战；本地学习的结构嵌入往往不匹配，简单的平均无法捕捉有意义的跨客户端关系。现有的大多数联邦图方法仅依赖结构聚合，忽视了大型语言模型（LLM）中丰富的全局语义上下文。本文提出了一种新颖的框架，使用LLM编码的知识来引导联邦图推荐。具体而言，客户端从本地图学习结构表征，同时通过冻结的LLM将其典型交互模式总结为紧凑的语义向量。中央服务器随后利用这些LLM编码的语义信号发现跨客户端的相关偏好模式，引导其结构表征的选择性聚合。这实现了语义驱动的跨客户端协作，同时不暴露原始数据。在标准基准上的大量实验表明，使用LLM编码知识引导结构对齐一致地提高了推荐准确性，优于现有联邦图基线。

## Abstract
Graph-based recommender systems are highly effective at extracting collaborative signals from user--item interactions, and federated learning (FL) allows these models to be trained while preserving user privacy. However, aggregating graph representations across distributed, non-IID clients remains a challenge; structural embeddings learned locally often misalign, and naive averaging fails to capture meaningful cross-client relationships. Most existing federated graph methods rely exclusively on structural aggregation, neglecting the rich, global semantic context available in large language models (LLMs). In this paper, we propose a novel framework that uses LLM-encoded knowledge to guide federated graph recommendation. Specifically, clients learn structural representations from local graphs while simultaneously summarizing their typical interaction patterns into compact semantic vectors via a frozen LLM. The central server then uses these LLM-encoded semantic signals to discover related preference patterns across clients, guiding the selective aggregation of their structural representations. This enables semantically informed cross-client collaboration without exposing raw data. Extensive experiments on standard benchmarks show that guiding structural alignment with LLM-encoded knowledge consistently improves recommendation accuracy over existing federated graph baselines.

---

## 论文详细总结（自动生成）

## 论文概述
- **标题**：Guiding Federated Graph Recommendation with LLM-encoded knowledge
- **核心问题**：在联邦图推荐中，客户端数据呈非独立同分布（non‑IID），本地学习的图结构嵌入容易错位，简单的参数平均无法捕捉有意义的跨客户端关系。现有方法主要依赖结构聚合，忽略了大型语言模型（LLM）蕴含的全局语义信息，导致推荐精度和稳健性不足。
- **整体含义**：提出一种语义引导的联邦图推荐框架，将 LLM 编码的语义知识作为跨客户端对齐的“软约束”，在不暴露原始交互数据的前提下，提升异质分布下的推荐性能与稳健性。

## 方法论
- **核心思想**：语义优先、结构调制的跨客户端协作。先利用冻结 LLM 从本地交互中提取语义描述，再以语义相似为条件选择性地聚合结构原型，避免将语义不同但结构相似的用户群落强行混合。
- **关键技术细节**：
  - **本地协同表征学习**：每个客户端使用轻量级 LightGCN 编码器从局部用户‑物品交互图中学习用户嵌入 \(h_u\) 和物品嵌入 \(h_i\)。
  - **自适应群落抽象**：以 HDBSCAN 对用户嵌入进行密度聚类，自动确定群落数量；对每个群落计算结构原型 \(P_{k,c}\)（群落内用户嵌入的均值），并选取群落内交互频率最高的前 \(L\) 个物品，将其文本元数据（标题、描述）拼接后用冻结 LLM 编码为语义向量 \(S_{k,c}\)。
  - **语义引导的结构聚合**：服务器计算群落间的语义相似度 \(s_{a,b}\)（余弦相似度）和结构相似度 \(r_{a,b}\)，组合为 \(q_{a,b}=s_{a,b}(1+\lambda r_{a,b})\)。仅当 \(q_{a,b}>\delta\) 时才将两个群落视为语义一致并允许合并，生成全局原型 \(\tilde{P}^{(t)}_g\)。
  - **对齐感知的本地微调**：客户端损失函数在 BPR 排序损失上增加对齐损失 \(\lambda_{\text{align}} \sum_{c} \|P_{k,c} - \tilde{P}^{(t)}_g\|_2^2\)，鼓励本地结构原型向语义对齐的全局原型靠拢。
- **算法流程**：每轮通信中，客户端执行本地图嵌入训练 → 自适应群落检测与语义编码 → 上传稀疏物品更新和群落描述符；服务器按行聚合物品嵌入，构建语义优先的群落图，合并符合条件的群落并广播全局原型；客户端随后进行对齐感知微调。

## 实验设计
- **数据集与场景**：三个基准数据集——MovieLens‑100K、MovieLens‑1M 和 Amazon Video（包含文本元数据），均转换为隐式反馈。用户按 IID 或 Dirichlet 分布（浓度参数 α=0.1, 0.3, 1.0）划分至多个客户端，模拟不同强度的 non‑IID 设置。评估采用留一法（last‑item），每个正样本与 100 个随机负样本排序，指标为 HR@10 和 NDCG@10。
- **对比方法**：
  - 集中式：MF、NCF、LightGCN。
  - 联邦非图：FedMF、FedNCF、PFedRec、FedRecon、MetaMF。
  - 联邦图：FedLightGCN、GPFedRec、GFedPP、UFGraphFR。
- **实现配置**：嵌入维度 d=64，LightGCN 层数 \(L_g=2\)，Adam 优化器（学习率 \(10^{-3}\)），每轮本地训练 5 个 epoch，HDBSCAN 最小群落大小 10，每个群落选取代表性物品数 \(L=10\)，LLM 编码器冻结且语义嵌入做 \(\ell_2\) 归一化，超参数 \(\lambda, \lambda_{\text{align}}, \delta\) 通过验证集调优。

## 资源与算力
- 论文未明确说明使用的 GPU 型号、数量或具体训练时长，仅提及使用冻结的预训练 LLM 编码器以控制通信和计算开销，未给出算力消耗的量化数据。

## 实验数量与充分性
- **主要实验**：在 non‑IID（α=0.3）下进行总体性能对比（1 张结果表，3 个数据集 × 约 11 种方法）。
- **异质性实验**：在 α=0.1, 0.3, 1.0 三种设置下对比代表性方法（2 张图，2 个数据集）。
- **消融实验**：分别移除语义引导、对齐损失、结构调制，考察各组件贡献（1 张图，3 个数据集）。
- **超参数敏感性**：分析 λ 和 \(\lambda_{\text{align}}\) 两个关键超参数对 HR@10 的影响（2 张子图，1 个数据集）。
- 实验覆盖了不同规模与稀疏度的数据集、多种 baseline、多维度消融及超参数分析，整体较为充分和客观，对比条件公平。

## 主要结论与发现
- SemFGRec 在所有数据集和 non‑IID 设置下均取得了最优的 HR@10 和 NDCG@10，相对最强基线 UFGraphFR 提升约 2 个百分点。
- 面对更强的数据异质性（α 减小），SemFGRec 的性能下降明显小于其他方法，表现出更优的稳健性。
- 消融实验证实语义引导是最关键的组件，对齐损失和结构调制均有正向贡献。
- 方法对关键超参数不敏感，易于调优。

## 优点
- **语义优先的策略设计**：利用冻结 LLM 提取语义描述，在保护隐私的同时为结构聚合提供全局信号，思路新颖且有效。
- **自适应群落检测**：采用 HDBSCAN 自动确定群落数量，避免人为预设，适应客户端异质性。
- **选择性聚合机制**：语义相似为前提、结构相似为调制，防止语义不一致的群落被错误合并，提升聚合的合理性与可解释性。
- **实验充分**：涵盖多个数据集、异构程度、基线方法以及组件消融和超参数分析，结果具有说服力。

## 不足与局限
- **实验覆盖**：仅测试了三个规模偏小的推荐数据集，未在大规模工业场景或更复杂的图结构（如社交图、知识图谱）上验证。
- **隐私分析有限**：虽声称保护隐私，但并未从差分隐私、安全多方计算等角度给出严格的隐私保证证明或通信代价与隐私风险的定量分析。
- **资源开销未量化**：未报告 LLM 编码器的推理延迟、通信量、存储开销，也未讨论在资源受限设备上的可行性。
- **语义表示依赖**：依赖物品文本元数据的质量与丰富度，若缺少描述性文本或文本质量较低，方法效果可能受限。
- **服务器端集中计算**：服务器仍需收集群落描述符并构建跨客户端图，存在潜在的瓶颈和单点风险，未探讨完全去中心化的可能。

（完）
