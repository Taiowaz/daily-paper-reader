---
title: "When the Tool Decides: LLM Agents Defer Blindly to Graph Neural Network Tools, and Stronger Backbones Defer More"
title_zh: 当工具做决定时：LLM智能体盲目遵从图神经网络工具，且更强的基座模型遵从程度更高
authors: "Zhongyuan Wang, Pratyusha Vemuri"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14476v1"
tags: ["query:graph-llm"]
score: 10.0
evidence: 直接研究LLM智能体配备GNN工具，属于GNN与LLM的集成
tldr: "近期工作将GNN作为LLM智能体的可调用工具，假设智能体具备判断何时及如何依赖工具的能力。本研究在ogbn-arxiv和WikiCS节点分类任务上，让ReAct式LLM智能体调用冻结的GNN工具，测量其遵从行为。实验显示智能体预测与GNN输出一致率高达99%，沦为复读机；遵从度随LLM骨干能力增强而提高，从0.60升至0.98；遵从代价在高同配性和替代工具更强时放大，简单选择性调用仅能部分恢复，且信息限制阻碍最佳选择调用。研究警示评估智能体+工具系统时不能假设智能体添加判断，选择性调用需显式设计，而非期望规模涌现。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14476v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1669, \"height\": 606, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14476v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 824, \"height\": 553, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14476v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1877, \"height\": 231, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14476v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1274, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14476v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 992, \"height\": 233, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14476v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1309, \"height\": 231, \"label\": \"Table\"}]"
motivation: 现有工作假设LLM智能体使用GNN工具时具备自主判断能力，但缺乏实证验证，本研究直接检验这一假设。
method: 在文本属性图节点分类任务上，将冻结GNN作为ReAct式LLM智能体的显式工具，测量其预测与GNN输出的一致率和遵从行为。
result: "智能体预测与GNN输出高度一致（97.6-99.2%），遵从度随骨干能力从0.5B到7B从0.60升至0.98，且遵从代价显著，选择性调用门仅部分恢复，受信息限制。"
conclusion: 评估智能体+工具系统不能假设智能体自主判断，选择性调用必须显式设计，缩放无法保证判断涌现。
---

## 摘要
越来越多的工作为大型语言模型（LLM）智能体配备图神经网络（GNN）作为可调用工具，并假设智能体能够自行判断何时以及多大程度上依赖该工具。我们对此进行直接测试。我们将一个冻结的GNN作为显式工具提供给ReAct风格的LLM智能体，并在文本属性图（ogbn-arxiv，并在WikiCS上复现）的节点分类任务上，衡量智能体是使用该工具还是仅仅服从它。我们发现智能体并没有运用判断力：其预测与原始GNN的输出在97.6-99.2%的情况下一致（5个随机种子），最终沦为“GNN鹦鹉”，全盘接受工具的输出，并绕过了自身的推理。通过扫描不同能力的基座模型（Qwen2.5 0.5B-7B），我们发现这种遵从并非弱模型的专属现象：在能够调用工具的模型中，与工具的一致性随模型能力提升而上升（从1.5B的0.60升至7B的0.98）。关键的是，随着智能体能力的增长，遵从的代价并未缩小，反而在备选方案出现时增大：一个针对可用动作的逐节点预言机，在3B模型上比“鹦鹉”高出0.09-0.18，在7B模型上高出0.12-0.22，在高同配性情境下几乎翻倍，这是因为“鹦鹉”被固定在冻结的GNN上，而智能体的其他选项在改善；在7B模型上，简单的邻居标签工具在高同配性下超越了GNN（0.81 vs 0.71），但智能体仍然选择遵从GNN。一个简单的选择性调用门限恢复了高同配性缺口的大约一半（从0.71到0.83），但并未带来净全局增益；留出法估计表明，基于标准测试时特征所能达到的最佳门限，最多只能弥补预言机额外提升的三分之一：可靠的选择性调用似乎受限于可用信息，而不仅仅是路由器的设计。我们的结果是一次警示性测量：对智能体+工具系统的评估不能假设智能体在工具之上增加了判断力，选择性调用必须被设计进去，而不能期望它通过扩展规模自发涌现。

## Abstract
A growing line of work equips large language model (LLM) agents with graph neural networks (GNNs) as callable tools, assuming the agent exercises judgment over when and how much to rely on such a tool. We test this directly. We expose a frozen GNN to a ReAct-style LLM agent as an explicit tool and measure, on node classification over a text-attributed graph (ogbn-arxiv, replicated on WikiCS), whether the agent uses the tool or merely obeys it. We find the agent does not exercise judgment: its predictions agree with the raw GNN's 97.6-99.2% of the time (5 seeds), collapsing into a GNN parrot that adopts the tool's output wholesale and bypasses its own reasoning. Sweeping backbone capability (Qwen2.5 0.5B-7B), the deference is not a weak-model artifact: among models able to invoke the tool, agreement rises with capability (0.60 to 0.98 from 1.5B to 7B). Crucially, the cost of deference does not shrink as capability grows and grows where alternatives emerge: a per-node oracle over the available actions beats the parrot by 0.09-0.18 at 3B and 0.12-0.22 at 7B, roughly doubling at high homophily, because the parrot is pinned to the frozen GNN while the agent's alternatives improve; at 7B a simple neighbour-label tool overtakes the GNN at high homophily (0.81 vs 0.71) yet the agent still defers. A simple selective-invocation gate recovers about half of that high-homophily gap (0.71 to 0.83) but yields no net global gain, and held-out estimates bound the best achievable gate over standard test-time features to at most a third of the oracle headroom: reliable selective invocation looks limited by available information, not merely router design. Our results are a cautionary measurement: evaluations of agent+tool systems cannot assume the agent adds judgment on top of the tool, and selective invocation must be designed in rather than expected to emerge from scale.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：当前研究普遍为大型语言模型（LLM）智能体配备图神经网络（GNN）等结构化预测器作为可调用工具，并隐含假设智能体能够自主判断何时、以多大程度依赖这些工具。本文直接检验这一假设是否成立，即当LLM智能体获得一个冻结的GNN工具时，究竟会将其作为证据之一进行权衡，还是毫无保留地服从工具的输出。
- **整体含义**：这是一项警示性测量研究。结果表明，智能体几乎完全沦为“GNN鹦鹉”，盲目遵从工具输出，且基座模型能力越强，这种遵从程度越高。这提示评估“智能体 + 工具”系统时不能假定智能体会贡献自主判断，选择性调用必须被显式地设计进去，而不能期望通过扩大模型规模自然涌现。

### 2. 论文提出的方法论
- **核心思想**：将经训练并冻结的GNN作为显式工具，嵌入ReAct风格的LLM智能体循环中，通过量化智能体最终预测与原始GNN输出的一致性来度量“遵从”程度，并借助“预言机差距”测量这种盲目遵从的代价。
- **关键技术细节与流程**：
  - **实验臂（Arms）**：设计四个条件以分离不同信号源：
    - **A1（智能体 + GNN工具）**：智能体可调用 `gnn_predict()`（返回预测标签与置信度）、`gnn_anomaly()`（返回重构异常分数，提示GNN可能出错的节点）、`gnn_link()`（返回邻居链接概率）三个子工具。
    - **A2（智能体 + 导航工具）**：智能体可调用 `neighbors()`（返回最多10个邻居ID及其训练集标签）和 `degree()`，不暴露邻居节点文本。
    - **A3（纯GNN）**：直接使用冻结的GNN进行预测。
    - **A4（纯LLM）**：智能体仅依靠节点文本（标题+摘要）进行推理，无图工具。
  - **遵从与代价度量**：
    - **遵从度（Parrot Effect）**：计算 A1 与 A3 预测标签的一致性比例。若该值接近1，则表明智能体在鹦鹉学舌。
    - **预言机差距（Oracle Gap）**：计算逐节点预言机 `max_acc(A1, A2, A4)` 与 A1 准确率之差，衡量智能体因盲目遵从GNN而放弃更优选项所付出的准确率代价。
  - **分层与扫描**：按节点本地同配性（邻居中同标签比例）划分低、中、高三个区间，展开细粒度分析。扫描Qwen2.5系列模型（0.5B、1.5B、3B、7B），考察模型能力与遵从行为的关系。
  - **选择性调用探索**：尝试后置门控，如基于邻居标签纯度的阈值门，以及训练基于四个特征（纯度、GNN置信度、度数、邻居分歧度）的路由器，以图恢复部分遵从代价。

### 3. 实验设计
- **数据集与场景**：
  - **主数据集**：ogbn-arxiv（文本属性引用图，约16.9万节点，40个论文类别），使用官方提供的标题+摘要作为节点文本。
  - **复现数据集**：WikiCS（维基百科计算机科学文章图，约1.17万节点，10个类别），用于检验结论的跨域可推广性。
- **基座模型**：Qwen2.5-Instruct系列（0.5B、1.5B、3B、7B），本地部署。边界条件测试额外包含Mistral-7B-Instruct和OLMo-2-7B-Instruct。
- **对比方法**：主要为内部设计的四个实验臂（A1–A4）之间的横向对比，以及不同能力模型间的纵向对比。对于选择性调用，对比了简单纯度门、学习到的四个特征路由器，以及信息天花板（如KNN最佳臂策略、中位数分箱策略）的估计值。

### 4. 资源与算力
- **算力详情**：论文未明确提及GPU型号、数量或具体训练时长。
- **可获取的信息**：
  - 提到大模型在本地服务器部署，受单GPU约束，因此未测试大于7B的模型。
  - GNN工具为2层GCN（隐藏层维度128），训练200个epoch，利用验证集选择最佳检查点。但未给出GNN训练的硬件开销。
  - 推理预算明确：每查询限制5000个提示+生成令牌和6次工具调用，使用vLLM或HuggingFace Transformers进行推理。

### 5. 实验数量与充分性
- **实验数量丰富**：
  - 两个数据集（ogbn-arxiv、WikiCS）。
  - 四类模型尺寸（0.5B、1.5B、3B、7B），其中3B、7B在**5个随机种子**下评估（重训GNN + 重采样节点），1.5B亦为5种子，0.5B作为能力底部参考。
  - 逐本地同配性区间（低、中、高）报告结果，每区间每次采样50个节点。
  - 包含选择性调用门控的多种形式实验（留一法训练、信息天花板估计）。
  - 界限条件测试两种非Qwen模型（3种子）。
- **充分性、客观性与公平性**：
  - 设计严密，通过多臂、多种子、分层分析和能力扫描，确保了结论的统计稳健性。
  - 公平性较好：各臂受控于完全相同的令牌/调用预算；A2使用的邻居标签与GNN训练集共享同样的监督信号，而非测试标签泄露。
  - 局限性在于，核心结论的“完全遵从”幅度在跨模型边界条件测试中表现出一定Qwen特异性（其他模型遵从度亦高但未达97%以上），但基本方向一致。

### 6. 论文的主要结论与发现
1.  **“鹦鹉”效应显著**：配备GNN工具的智能体预测与原GNN输出高度一致（在ogbn-arxiv上为97.6%–99.2%），几乎完全绕过了自身基于文本的推理能力。
2.  **能力反直觉地加深遵从**：在能正常调用工具的智能体中（≥1.5B），与GNN的一致性随模型参数量的增加而上升（平均值从60%升至98%），更大的模型非但未变得更审慎，反而更彻底地服从工具。
3.  **遵从代价可观且未随能力缩减**：在完全遵从（3B、7B）的区间内，预言机差距仍然显著，甚至在高同配性节点上因7B模型的替代方案（如邻居标签导航工具）性能反超GNN而加倍。更强的智能体因为盲从GNN而放弃了自身能力所带来的更优选项。
4.  **选择性调用困难且受信息限制**：简单的后置门控或学习型路由器仅能恢复部分损失（在高同配性下约一半），且全局无增益。信息天花板分析表明，基于标准测试时特征所能恢复的增益不足预言机额外提升的三分之一，可靠的选择性调用似乎受限于可用信息本身，而不只是路由器设计。

### 7. 优点
- **问题精准，直指隐含假设**：对“工具化LLM智能体具备判断力”这一广泛潜在的假设进行了直接、可证伪的测量，填补了基础行为认知的空白。
- **实验设计严格、多维度分解**：
  - 多臂设计巧妙分离了GNN、结构导航和纯LLM推理的信号。
  - 分层分析揭示了不同网络环境下行为模式的统一性（始终遵从）和代价的异质性。
  - 能力扫描实验提供了一个反直觉且重要的证据：规模增大并未带来工具使用的“智慧”。
- **深入探究局限**：不仅发现失败模式，还通过信息天花板分析等手段，将“为什么”失败的部分原因归结到可用信息层面，深化了对该开放问题的认识。
- **结论稳健，注重可复现**：采用多随机种子、双数据集复现、控制预算等做法，并以共享节点样本进行配对比较，强化了统计结论的可靠性。

### 8. 不足与局限
- **模型与语言单一性**：主要结论基于Qwen2.5家族（提示为中文）。跨模型边界测试表明，其他模型虽然也表现出工具遵从倾向，但未达到Qwen几乎100%的极端程度，说明结论的量化幅度可能存在模型特异性。
- **GNN工具的单一性**：工具仅限为简单的GCN模型，未扩展到其他更高级的GNN架构。结论在更复杂或更强大的图工具下是否依然成立有待验证。
- **任务与图类型有限**：只在同配性居中偏高的节点分类引用/文章图上进行了验证，未在异配性极强的图上进行压力测试（尽管论文提及作为未来工作），这可能会限制有关“代价放大”结论的覆盖范围。
- **选择性调用未提供可行解法**：所测试的门控方案均未带来全局增益，虽然信息天花板分析为失败提供了解释，但论文本身并未提出一个能可靠解决遵从问题的选择性调用机制，止于警示和问题界定。
- **资源透明度不足**：未报告具体的硬件配置与训练/推理时间，使得计算开销评估不够清晰。

（完）
